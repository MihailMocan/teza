{
  "name": "licenta",
  "version": "1.0.0",
  "description": "Teza de licenta",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": " cd client && npm i && npm start",
    "backend": "cd server && npm i && npm start ",
    "start": "concurrently \"npm run client\" \"npm run backend\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fortawesome/fontawesome-svg-core": "^6.4.0",
    "concurrently": "^8.0.1"
  }
}
